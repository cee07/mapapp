<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 xmlns:helper="clr-namespace:mapapp.Helpers"
			 x:Class="mapapp.Views.BadgeValueView">
	<ContentView.Content>
		<!--<AbsoluteLayout HorizontalOptions="FillAndExpand" BackgroundColor="Fuchsia">
			<helper:CustomProgressBar VerticalOptions="CenterAndExpand"
									  BackgroundColor="Aqua"
									  AbsoluteLayout.LayoutFlags="All"
									  AbsoluteLayout.LayoutBounds="0.5,0.5,1,1"
									  Progress="1"/>
			<Label Text="{Binding Title}" BackgroundColor="Transparent"
				   HorizontalOptions="Center" VerticalOptions="Center"
				   VerticalTextAlignment="Center" HorizontalTextAlignment="Center"
				   TextColor="White"
				   AbsoluteLayout.LayoutFlags="All"
				   AbsoluteLayout.LayoutBounds="0.5,0.5,1,1"/>
		</AbsoluteLayout>-->
		<!--<StackLayout>
			<Label Text="{Binding Title}" BackgroundColor="Transparent"
				   TextColor="#77A3B1"
				   HorizontalOptions="Center" VerticalOptions="Center"
				   VerticalTextAlignment="Center" HorizontalTextAlignment="Center"
				   />
			<ProgressBar Progress="{Binding Progress}" ProgressColor="#77A3B1"/>
		</StackLayout>-->
		<StackLayout>
			<Image Source="{Binding Badge}" />
		</StackLayout>
	</ContentView.Content>
</ContentView>
