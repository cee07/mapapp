<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="mapapp.Views.SubmitPinsListView">
	<ContentView.Content>
		<Grid>
			<ScrollView>
				<StackLayout x:Name="submittedPinsParent"/>
			</ScrollView>
			<AbsoluteLayout IsVisible="{Binding IsBusy}" BackgroundColor="Transparent"
							HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
				<ActivityIndicator IsRunning="{Binding IsBusy}"
								   IsVisible="{Binding IsBusy}"
								   AbsoluteLayout.LayoutFlags="All"
								   AbsoluteLayout.LayoutBounds="1,1,0.5,0.5"/>
			</AbsoluteLayout>
			<AbsoluteLayout BackgroundColor="Red"
							HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
				<ImageButton
								   AbsoluteLayout.LayoutFlags="All"
								   AbsoluteLayout.LayoutBounds="1,1,1,0"/>
			</AbsoluteLayout>
		</Grid>
	</ContentView.Content>
</ContentView>
