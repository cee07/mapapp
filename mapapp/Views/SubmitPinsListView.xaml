<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="mapapp.Views.SubmitPinsListView">
	<ContentView.Content>
		<Grid>
			<ScrollView VerticalOptions="FillAndExpand">
				<StackLayout x:Name="submittedPinsParent" BackgroundColor="Transparent"
							 VerticalOptions="FillAndExpand"/>
			</ScrollView>
			<AbsoluteLayout BackgroundColor="Transparent"
							HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
				<ActivityIndicator IsRunning="{Binding IsBusy}"
								   IsVisible="{Binding IsBusy}"
								   AbsoluteLayout.LayoutFlags="All"
								   AbsoluteLayout.LayoutBounds="1,1,0.5,0.5"/>
			</AbsoluteLayout>
		</Grid>
	</ContentView.Content>
</ContentView>
